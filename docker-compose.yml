services:
  cs-bot:
    build: .
    container_name: cs-bot
    restart: unless-stopped
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - ./knowledge_base:/app/knowledge_base:ro
      - ./memory:/app/memory
      - ./data:/app/data
